services:
  codenames:
    build: .
    ports:
      - "3000:3000"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - SPYFALL_IMAGE_BASE=${SPYFALL_IMAGE_BASE:-}
      - SPYFALL_IMAGE_EXT=${SPYFALL_IMAGE_EXT:-jpg}
